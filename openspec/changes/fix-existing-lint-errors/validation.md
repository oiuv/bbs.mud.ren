# 验证记录

日期：2026-09-18。环境：Windows、Node.js 24.21.0、npm 12.0.2，本地开发地址 `http://127.0.0.1:8081/`。

## 自动检查

| 检查 | 结果 |
| --- | --- |
| `npm run lint:check` | 通过，退出码 0；119 errors / 532 warnings → **0 errors / 530 warnings** |
| `node scripts/check-ui-state.cjs` | 23 项通过，0 项失败；不访问网络 |
| `node node_modules/eslint/bin/eslint.js scripts/check-ui-state.cjs` | 通过，0 errors / 0 warnings |
| `npm run build` | 通过，生成 `dist/`；提交前构建 hash `7a32ae1a96eb0dd7` |
| `openspec validate fix-existing-lint-errors --strict` | 通过 |
| `git diff --check` | 通过 |

ESLint 规则、检查范围、依赖版本与环境配置没有更改。剩余警告以模板格式、属性排列、组件选项顺序及命名约定为主；其中 510 项可自动修复，本次没有批量格式化这些警告。生产构建仍有警告，不等于零警告构建。

回归脚本使用项目已有 Vue 编译器和 Babel，以及 Node 内置测试能力。覆盖冻结输入对象后的关系切换、失败不发更新事件、点赞仅增减一次、包装组件事件转发、分页请求与失败保留数据、动态过滤，以及两种通知的格式化内容和路由。

## 浏览器验收

使用独立的 Playwright / Edge 会话，不复用用户登录状态。

| 场景 | 结果 |
| --- | --- |
| 真实公共接口下的游客首页和 `/threads/1` | 页面和内容正常加载；未发现浏览器运行错误，有已有的 Vue Router `tag` 弃用警告 |
| 游客点击桌面浮动点赞入口 | 跳转 `/auth/login` |
| 390 × 844 的主题页 | 文字及底部操作栏正常显示，已检查截图 |
| 模拟登录后的普通点赞与浮动点赞 | 计数 7 → 8 → 7，两个入口状态同步；随后模拟 422，计数保持 7 |
| 主题作者卡与侧栏关注按钮 | 关注和取消关注后两个入口同步；模拟 422 后保持未关注 |
| 订阅按钮 | 订阅、取消订阅与失败保留状态均通过 |
| 粉丝、关注列表分页 | 点击第 2 页发出 `page=2` 请求并显示对应用户及页码；第 3 页模拟 422 后仍显示第 2 页 |
| 粉丝、关注列表中的关注按钮 | 关注和取消关注正常回写列表项 |
| 390 × 844 的关注列表 | 内容宽度与视口均为 390，无横向溢出，已检查截图 |

认证交互使用模拟 `/oauth/token`、`/me` 和关系操作响应。所有 API 写请求均由浏览器拦截，公共 GET 请求移除模拟认证头；没有向线上账号写入数据。失败场景产生预期的 422 网络记录和 Vue 异步事件错误日志，页面保持原状态并显示失败提示。

## 验证边界与本机证据

- 未使用真实账号验证生产认证、真实关系写入或 CAPTCHA；模拟测试验证的是前端状态流，不能代替这些后端验收。
- 窄屏主题页左侧浮动工具条仍沿用原有负边距位置，位于视口外；本次没有调整布局，主题底栏的普通点赞入口仍可见。
- 临时日志、浏览器验证脚本和截图保存在本机 `%TEMP%\mudren-local\`，不进入源码提交。截图为 `lint-guest-desktop.png`、`lint-guest-mobile.png`、`lint-relations-desktop.png` 和 `lint-pagination-mobile.png`；提交前复核日志为 `lint-precommit.log` 和 `build-precommit.log`。
- 本地开发服务保持运行，`dist/` 已更新。OpenSpec 变更保留以供审阅。
