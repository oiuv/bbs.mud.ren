# Tasks

## 1. Theme behavior

- [x] 1.1 Add early theme initialization, persistence and cross-tab synchronization; verify default, invalid values and storage failures with Node checks.
- [x] 1.2 Add a labeled navigation toggle for guest/authenticated desktop/mobile views; verify keyboard switching, reload persistence and unchanged form input in the browser.

## 2. Forum appearance

- [x] 2.1 Add dark/light tokens and adapt shared shell, Bootstrap, Markdown, CodeMirror and Element UI surfaces; verify computed colors and screenshots in both modes.
- [x] 2.2 Replace scoped hardcoded exceptions in categories, lists, pagination, AI shell, notifications and floating actions; verify affected routes without inverted images or overflow.

## 3. Validation

- [x] 3.1 Run lint, existing state/rich-content regression, theme checks, production build and strict OpenSpec validation; record results and remaining boundaries.
- [x] 3.2 Inspect desktop/mobile screenshots and guest/authenticated/error states with all API writes mocked; document usage, evidence and external-embed limitations.
